package com.wagner.cashbook.repository;

import com.wagner.cashbook.model.UserModel;
import org.springframework.stereotype.Repository;

@Repository
public class UserRepository {
    public void persist(UserModel userModel) {
        // persist db
    }
}
